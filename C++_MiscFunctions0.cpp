#include <iostream>
#include <algorithm>
#include <vector>

int main(void) {
    std::vector<int> v1 = { 1, 5, 7, 3, 8, 3 };

    std::vector<int> v2(6);
    std::vector<int> v3(6);

    // Using copy() to copy 1st 3 elements
    std::copy(v1.begin(), v1.begin() + 3, v2.)
}